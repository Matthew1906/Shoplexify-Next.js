'use client'

import { TextButton } from "@/app/components/utils";

const ReviewButton = ()=>{
    return <>
        <TextButton text="Add Review" onClick={()=>{}}/>
    </>
}

export default ReviewButton;
